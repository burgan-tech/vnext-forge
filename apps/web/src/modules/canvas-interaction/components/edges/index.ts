import { TransitionEdge } from './TransitionEdge';

export const edgeTypes = {
  manualEdge: TransitionEdge,
  autoEdge: TransitionEdge,
  scheduledEdge: TransitionEdge,
  eventEdge: TransitionEdge,
  sharedEdge: TransitionEdge,
};
