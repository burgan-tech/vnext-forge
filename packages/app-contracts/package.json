{
  "name": "@vnext-studio/app-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared app contracts for API responses and error handling.",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
