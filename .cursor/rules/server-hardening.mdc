---
description: Hardening invariants for the active Hono RPC backend (`apps/server`). Auto-loads when editing files under `apps/server/src/**`. Body limit, CORS allowlist, capability policy, runtime-proxy SSRF defense, filesystem jail, LSP WebSocket policy, error-handler invariants, and the `trace-v1` contract must stay intact. See ADR 001 for trust model.
globs:
  - "apps/server/src/**/*.ts"
alwaysApply: false
---

# Server Hardening Rule

> **Loads when editing**: any file under `apps/server/src/**`. Authoritative ADR: [`docs/architecture/adr/001-trust-model.md`](../../docs/architecture/adr/001-trust-model.md).

## Hard invariants (do not regress)

1. **Default bind = loopback (`127.0.0.1`).** A non-loopback bind requires explicit config and the items listed under `BLOCKER-FOR-NON-LOOPBACK` in ADR 001. Don't change the default.
2. **CORS** uses an **explicit allowlist** (no `*` reflection). Lives in `apps/server/src/shared/middleware/cors.ts`. Adding an origin requires a config entry, not a code default.
3. **Body limit + JSON parse** middleware (`shared/middleware/body-limit.ts`) runs before any handler that reads JSON. RPC requests over the limit return `ApiFailure` with `RPC_BODY_TOO_LARGE`.
4. **Trace headers (`trace-v1`)** — `trace-id` middleware runs first; the server generates `X-Trace-Id`; inbound `traceparent` becomes `linkedTraceId` only. See `.cursor/skills/shared/trace-headers/SKILL.md`.
5. **Error handler** is the **single** translation boundary from `ZodError` / `VnextForgeError` to `ApiResponse<never>`. Route handlers do not hand-craft failure JSON. See `.cursor/skills/shared/error-taxonomy/SKILL.md`.
6. **Per-method capability policy** is enforced inside the RPC dispatcher; capabilities (`reads-files`, `writes-files`, `spawns-process`, `talks-runtime`) are declared on the registry, not on the route. Adding a capability requires updating the registry, not bypassing the dispatcher.
7. **Runtime-proxy URL allowlist** (`packages/services-core/src/services/runtime-proxy/`) — the proxy resolves the inbound URL against the allowlist and rejects anything else. Don't add a "passthrough" branch.
8. **Filesystem jail** — file reads/writes go through the workspace-scoped resolver that runs `realpath` and rejects symlinks escaping approved roots.
9. **LSP WebSocket policy** — max message bytes, max connections, origin check (when not bound to loopback). Live in `apps/server/src/lsp/router.ts` + `lsp-ws-policy.ts`.
10. **Child processes** use the env allowlist via `buildChildEnv()` — don't spread `process.env` into a child.
11. **Logging** flows only through the central logger (`baseLogger`, `getRequestLogger`). No `console.*` in app code.

## Adding a new endpoint or RPC method

- Prefer adding an RPC method to `packages/services-core` over a new HTTP route. The HTTP layer is intentionally thin.
- New RPC methods need: `paramsSchema`, `resultSchema`, capability tag, JSON fixture under `packages/services-core/test/fixtures/`, and pass the snapshot test.

## Don'ts

- Don't disable middleware "just for tests" — write the test against the real middleware chain.
- Don't reintroduce `cors({ origin: '*' })` even temporarily.
- Don't read or set `X-Trace-Id` from inbound requests except to log it as `linkedTraceId`.
- Don't bypass the registry's capability check from inside a service.

## Cross-references

- [`docs/architecture/adr/001-trust-model.md`](../../docs/architecture/adr/001-trust-model.md) — full trust model.
- [`docs/architecture/adr/002-trace-headers.md`](../../docs/architecture/adr/002-trace-headers.md)
- [`docs/architecture/adr/005-error-taxonomy.md`](../../docs/architecture/adr/005-error-taxonomy.md)
- `.cursor/skills/shared/error-taxonomy/SKILL.md`, `trace-headers/SKILL.md`.
